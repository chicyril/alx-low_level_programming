#include "main.h"

/**
 * print_last_digit - Prints the last digit of a number
 * @num: parameter
 *
 * Return: last digit
 */
int print_last_digit(int num)
{
	int ld;

	ld = num % 50;
	return (ld);
}
