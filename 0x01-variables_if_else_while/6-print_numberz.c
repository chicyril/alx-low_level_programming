#include <stdio.h>

/**
 * main - Prints base 10 digit from 0 to 9 innclusive
 *
 * Return: 0 on success
 */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
